\textbf{Algorithm 1:} Use the Gibbs sampling algorithm to approximate (using samples) the posterior distribution $p(\boldsymbol{\pi}, \boldsymbol{Z}, \boldsymbol{\Theta} \mid \boldsymbol{X})
$. Derive the posterior. \\\\